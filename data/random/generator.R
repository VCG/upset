# Random Set Generator for UpSet

membership = function( p ) {
  return ( runif( ))
}

# number of items to generate
i <- 1000;

# number of sets to generate
s <- 10;

# probability of an item to be contained in any given set
p <- 0.5; 

sets <- matrix( 0, nrow=i, ncol=s );



